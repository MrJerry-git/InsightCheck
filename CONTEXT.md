# 领域词汇表

## 核心对象

- **受检者（Patient）**：以匿名编码标识的健康数据主体；不包含身份证、手机号或住址等直接身份信息。
- **体检批次（HealthCheck）**：某位受检者在一个日期、一个机构形成的一次体检事件，是实验室指标、影像检查和已检项目的共同时间锚点。
- **实验室指标观测（LabMetric）**：一次体检批次中的单项数值观测，同时保留原始名称、原始值、原始单位和标准化结果。
- **指标字典（MetricDictionary）**：定义指标稳定编码、规范名称、别名、标准单位、可支持的单位换算和基础有效值边界的版本化语义来源。
- **影像检查（ImagingExam）**：一次体检批次中的一项影像检查及其原始报告文本。
- **病灶观测（Lesion）**：某次影像检查中记录的一处病灶；它是单次观察，不等同于跨年稳定病灶身份。
- **病灶轨迹（LesionTrack）**：同一受检者名下、经明确方法关联的一组跨期病灶观察的稳定身份。
- **病灶轨迹观察（LesionObservation）**：病灶观测与病灶轨迹之间的一次带状态、置信度和日期的关联。
- **匹配候选（Candidate Match）**：当前病灶与某条历史轨迹之间的可解释评分关系；候选不等于已确认归轨。
- **需要复核（NEED_REVIEW）**：置信度处于复核区间，只保留候选轨迹，不能写入已匹配轨迹身份。
- **轨迹未决（UNRESOLVED）**：当前证据不足以判断轨迹继续或消失的状态。
- **体检项目（ExamItem）**：可被检查历史、医疗规则和未来推荐共同引用的规范项目目录条目。
- **已检项目（ExamHistory）**：体检批次与体检项目之间的实际发生记录。
- **医疗规则（MedicalRule）**：带来源、版本、优先级和启用状态的项目约束定义；规则不是模型评分。
- **风险预测（RiskPrediction）**：未来风险模型针对受检者、时间锚点和风险类别产生的版本化概率输出。
- **体检方案（Recommendation）**：未来推荐流程生成的一档方案，关联模型版本、规则版本和处理追踪标识。
- **方案项目（RecommendationItem）**：体检方案中的单个项目决定，分别保留模型分数、规则处理后的分数、证据和适用规则。
- **AI 报告（AIReport）**：LLM 基于结构化上下文形成的解释性文本，不拥有风险判断或项目推荐权。
- **指标纵向特征（MetricLongitudinalFeatures）**：一个规范指标在指定截止日期前的多次有效观测所形成的统计、回归趋势、异常计数和参考界限距离，并保留来源证据。
- **患者特征向量（PatientFeatureVector）**：按版本化流水线生成的指标解释对象、系统分组和稳定顺序数值字典；是未来风险模型的输入契约，不是风险结论。
- **趋势（MetricTrend）**：由回归斜率、首尾变化、波动程度和方向一致性共同形成的 `RISING`、`FALLING`、`STABLE` 或 `FLUCTUATING` 描述性结论；证据不足两次时显式为 `UNKNOWN`。
- **早期提示（EARLY_WARNING）**：指标仍在参考区间内，但最近连续同向变化并逐步接近同侧参考界限的可解释提示；不等同于指标异常或疾病诊断。
- **候选项目集合（CandidateSet）**：进入 DeepFM 前的高召回体检项目集合；当前不执行任何医疗安全过滤，并明确要求后续 Rule Engine。
- **DeepFM 匹配分数（deepfm_score）**：Patient × ExamItem 的 0~1 匹配排序值；不是疾病概率，也不能绕过规则引擎成为最终推荐。
- **推荐训练标签（Recommendation Label）**：未来窗口内观察到的项目交互标签；历史开具行为带有机构、医生、费用和可及性偏差，不等价于医学需要真值。
- **推荐模型制品（Recommendation Model Artifact）**：共同保存权重、embedding 配置、特征映射、模型/特征/数据版本和 Demo 标记的可复现目录。

## 关键约束

- 原始体检观测与标准化结果必须同时保留，标准化不能覆盖源值。
- `is_demo = true` 表示合成演示数据；演示数据不得作为真实模型实验或医疗结论。
- 病灶术语统一只改变部位表达，不自动证明两个年度的病灶属于同一轨迹。
- 同一检查批次内病灶与历史轨迹必须一对一分配；低置信度病灶不得静默归轨。
- 风险概率不得表述为确定诊断；体检方案最终需要有资质医务人员确认。
- 核心输出必须能追溯到输入证据、处理版本和一次运行标识。
- 指标多年趋势不得只由最近两次差值决定；单次记录必须显式标记 `has_history=false`。
- 特征层不做静默插值；缺失观测必须计数并保留来源引用。
- DeepFM 不执行复查间隔、辐射、禁忌或功能重复规则；候选和模型输出必须继续进入 Rule Engine。
- 未加载推荐模型制品时必须显式失败，不得生成随机或写死匹配分数。
