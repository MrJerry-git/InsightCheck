# Synthea 小样本实测记录

审计日期：2026-09-16。数据来源：[Synthea 官方下载页](https://synthetichealth.github.io/downloads.html)。这是合成数据工程验证，不能作为医学有效性实验。

文件 5,960,866 字节；SHA256：d61417b551e5b0997c33851b339c157421751f0ea68c18ea686ceb1850907c35。官方页面的“100 人”是样例名称，本次实际文件有 108 名患者，按实测记录。

| 对象 | 原始数量 | 本次导入 |
| --- | ---: | ---: |
| 患者 | 108 | 108 |
| 就诊 | 5571 | 1262 |
| 观测 | 68648 | 4396 |

只选 wellness；其余急诊、住院等未当作体检。观测排除数：非目标就诊 40294、非数值 12614、未映射代码 11344；与导入 4396 合计 68648。

导入包含身高 900、体重 900、BMI 796、收缩压 900、舒张压 900 条。没有临床参考区间，也没有报告可获得时间；状态保留 unknown。身份字段未进入标准导入包。

本机从空数据库迁移后，首次导入新增 108/1262/4396；重复导入新增 0/0/0，复用 108/1262/4396。两次 batch_id 相同。原始包、转换数据和数据库均在 Git 忽略范围内。

复现方式见 [导入说明](SYNTHETIC_IMPORT.md)。latest 下载内容可能变化，复现时先比较哈希。本报告不代表已取得合作方数据，也不证明 NLST 纵向任务已经可行。

## 工程验证

- 后端 123 项测试通过，Ruff 检查通过；空数据库迁移成功，Alembic 模型差异检查通过。
- 前端 TypeScript、ESLint、2 项已有测试和生产构建通过。
- 浏览器实测文件校验、确认导入、重复导入复用、108 人列表、个人历史展示及刷新后选择状态重置。
- 测试覆盖错误单位、非有限数值、跨患者引用、无时区时间、重复行、意外身份列、来源记录冲突及写入失败整批回滚。
- 当前测试环境有两项上游测试客户端弃用提示，不影响本次通过结果；后续升级依赖时处理。
