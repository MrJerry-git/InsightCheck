# 仓库内容审查

日期：2026-09-16。基线：6803b3d。目标是减少误导和维护成本，而非按文件大小或暂未调用就删除。

## 当前判断

| 内容 | 判断 | 处理 |
| --- | --- | --- |
| 后端数据模型、迁移、特征、规则、DeepFM 与测试 | 有实际实现价值 | 保留并按实施设计修正边界 |
| 独立 HTML Demo | 交互参考，不是真实计算入口 | 暂保留；后续移入设计归档并纠正完成状态文案 |
| 12 个业务占位页 | 提供路由，但不代表功能 | 后续首版导航收敛，不继续扩写占位功能 |
| 推荐详情静态规则结果 | 组件示例，可误认为真实结果 | 后续换成真实接口；静态数据移入演示夹具 |
| ARCHITECTURE 与前后端 README 的阶段说明 | 存在过时、互相矛盾内容 | 本次修正入口说明，历史章节标为阶段记录 |
| LLM 接口及 AI 页面 | 未来扩展，首版不需要继续投入 | 保留边界，暂缓供应商接入 |
| lesion_terminology.py | 旧路径兼容层，已有调用者 | 保留；迁移调用并验证后才能删除 |
| AGENTS.md / CLAUDE.md | 工具协作说明与引用入口 | 保留，不因文件短而删除 |
| package-lock.json、Alembic 历史、测试与快照 | 复现和回归所需 | 保留，不能当作杂物 |

旧环境、缓存、数据库、提示词文档和原申报书不在当前 Git 提交中。iCan 原目录是工作区外的历史备份，不应与 InsightCheck 同时开发。

本次不删除业务文件；没有完成调用与界面回归前不做批量清理。下一次工程任务优先：统一状态、隐藏不属于首版的导航、分离静态演示、检查残留模板资源；确认无引用后再移除。

“暂不实现”与“没有意义”不同。最需要清理的是过时状态和虚假完成感，不是已有可复用模块。
