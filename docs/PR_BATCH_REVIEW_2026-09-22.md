# PR 批次审核与协作接线记录（2026-09-22）

本记录是代码审核结果，不是 2.0 或临床效果验收。审核按 TEAM_TASKS_V2.md、TASK_WTY_CONVERSATIONAL_IMPORT.md 和 main 既有参赛前端共同核对。

## 已合并
- [#11](https://github.com/MrJerry-git/InsightCheck/pull/11)：H01/H02 检查与关联字典；医学内容仍待审核。
- [#14](https://github.com/MrJerry-git/InsightCheck/pull/14)：13 页比赛计划书空白模板；不计为陈子正前端任务完成。
- [#25](https://github.com/MrJerry-git/InsightCheck/pull/25)：H09 模型任务注册基础设施；不等于模型或管理页面已接入。
- [#27](https://github.com/MrJerry-git/InsightCheck/pull/27)：H11 九华数据到达前准备；不表示已接入真实数据。
- [#40](https://github.com/MrJerry-git/InsightCheck/pull/40)：参赛前端橙色主题、线条背景、人体透明度及去除器官阴影。

## 暂未合并
| PR | 决定 | 首要原因 |
| --- | --- | --- |
| #19 | 要求修改 | 未提供单位仍赋予标准单位 |
| #20 | 要求修改 | 扫描 PDF 未调用 OCR、混合 PDF 丢失无文本页 |
| #21 | 要求修改 | 未知单位仍计算趋势 |
| #23 | 要求修改 | 独占一行的报告段落标题导致 KeyError |
| #24 | 要求修改 | 冲突失败项仍在候选；缺年龄仍应用受限规则 |
| #26 | 要求修改 | 引用编号未与内容绑定；真实模型接线未验收 |
| #28 | 要求修改 | 组件接入状态、既有 PREVENT/Qwen 与运行证据未对齐 |
| #29 | 依赖暂缓 | 汇总 PR 含未修复的 H 模块，不能绕过单项审核 |
| #30 | 要求修改 | 权限遗漏参赛报告接口与 RecommendationItem |
| #31 | 依赖暂缓 | 依赖 #30，尚待与 H 数据契约及独立参赛档案联调 |
| #32 | 要求修改 | 缺 python-multipart 运行依赖；Excel/H 解析接线未完成 |
| #33 | 要求修改 | 漏提交 analysis_finding_map.json；患者字段变化未使分析过期 |
| #34 | 要求修改 | 方案复算复用旧规则结论 |
| #35 | 要求修改 | 人工映射改单位标签但未换算值；可用状态只看配置路径 |
| #36 | 要求修改 | 幂等请求可返回他人问答；历史报告证据读取当前值 |
| #37 | 要求修改 | 按字符串顺序比较 Alembic 迁移版本 |
| #38 | 要求修改 | 干净检出链路失败，H 服务与参赛页面尚未真实接通 |
| #39 | 要求修改 | 旧确认请求可确认更新后的草稿；权限/前端接线未完成 |

详细代码位置、复现、修改建议已发布在各 PR 对应提交的审核意见中。以上状态仅为本次审核时点，后续修复以 GitHub 最新审核为准。

## 必须共同完成的接口链路
1. 王宏锦的 H03/H04 输出必须经王天一的 T03 校对确认后入库。原值、单位未知、未映射项目不能丢失或猜测。
2. H05/H07 的异常、候选、排除和缺项必须进入 T04/T05/T07 同一链路；冲突排除项不能在规划阶段重新出现。
3. H08 与 T10 统一模型适配和引用映射；H09 与 T08 统一实际可用状态，区分未配置、加载失败和已接入。
4. AI #39 的 profile、session、draft_version、confirmed version、op_id 与 T01 权限共同设计；旧请求不能覆盖新会话，跨账号/档案不可串数据。
5. 陈子正负责前端接线，保留当前橙色视觉。main 的人体页面仍调用旧 /smart-import 接口，不能因 #39 后端存在就宣布对话编辑/新建档案完成。
6. 页面状态以服务端已确认数据为准：资料确认→人体点亮→系统历史→该系统规划→整体规划→保存回看。缺项以对话追问；未知不能显示正常；人体点亮不代表健康。
7. 新建档案采用 AI 工作区样式，含保存提醒、取消保留、切换后清空当前工作区与人体复位、迟到响应隔离，保留已保存规划。与“重新开始整理”分开。
8. 参赛页面不增加 JSON、原文依据或直接编辑表单；旧版历史验收记录不恢复这些交互要求。

## 验证证据与边界
- 已合并的 #11/#14/#25/#27 与 main #40 在独立副本联合验证：后端 **321 passed**，差异空白检查通过。该验证不包括被阻断的 T 链或 AI #39。
- T 系列顶端 bf87015：先因 python-multipart 缺失不能加载应用，审核环境临时补齐后 **325 passed / 20 failed**。缺失的映射配置使分析、方案、报告与端到端测试失败。
- H 系列及 AI 原有模块测试能够通过；追加反例仍复现未知单位趋势、段落 KeyError、无效冲突排除和旧草稿确认错误。不能把原有测试全绿等同于业务完成。
- 两个独立账号使用相同问答 request_id，第二账号实际收到第一档案的问答；已在临时数据库复现。
- 使用真实迁移标识 f2a7c4d10e88→b3c1d9e77a45，在临时库复现错误允许旧备份覆盖新迁移版本。
- 所有复现数据均为人工构造；没有操作真实患者数据或覆盖正在运行的数据库。

## 建议修复顺序
先修单位/抽取与权限、干净安装、缺配置；随后统一 H/T 契约和 AI 草稿版本保护，再做数据库升级及前端联调。T 系列基础 PR 合入 main 后再调整下一条 PR 的基分支并复核，不将整条未通过链一次性带入 main。
